# AFTERIMAGE Web

Set `NEXT_PUBLIC_API_URL` to your API origin. Without it, the app runs fully in demo mode using deterministic afterimages.

```bash
npm install
npm run dev
```

Scripts: `dev`, `build`, `start`, `lint`, `typecheck`.
